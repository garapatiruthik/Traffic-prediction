
# =============================================================================
# requirements.txt — Traffic Forecasting Thesis Pipeline
# BTH Blekinge Institute of Technology — Computer Science Bachelor Thesis
# =============================================================================
# System: Windows / Linux / macOS  |  Python ≥ 3.10
#
# Core (ML framework)
# ───────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
requests>=2.28

# Deep learning (PyTorch)
# Install separately first to get the right CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# HuggingFace Transformers — required for Chronos-2 / ChronosPipeline
transformers>=4.40

# Optional acceleration helpers
# accelerate>=0.20           # multi-GPU / CPU offload for model loading
# click>=8.0                 # future CLI extensions
